# Core deep learning stack. Install the appropriate CUDA PyTorch wheels first.
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0
transformers>=4.45.0
tensorboard>=2.16.0

# Configuration and utilities
deepspeed
pyyaml>=6.0.0
tqdm>=4.66.0

# Qwen tokenizer dependencies
sentencepiece>=0.1.99
tokenizers>=0.15.0

# Distributed training
accelerate>=0.30.0

# Tests
pytest>=8.0.0
